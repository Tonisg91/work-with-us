<main id="announcements-main">
  <form action="#" method="POST" id="searchbar-form">
    <div>
      <input type="text" placeholder="Busca un anuncio...">
      <button type="submit"><i class="fas fa-search"></i></button>
    </div>
    <div>
      <a href="/addAnnouncement" id="add-button"><i class="fas fa-plus-circle"></i></a>
    </div>

  </form>
  <section id="announcements-list">
    {{#each list}}
    <div class="announce-card">
      <img src="{{this.photoCard}}" alt="Imagen del anuncio">
      <a href="/announcement/{{this._id}}">
        <div class="announce-title">
          <h4>{{this.title}}</h4>
        </div>
        <p class="state">{{this.location.state}}</p>
        <p class="city">{{this.location.city}}</p>
        <p class="tags">{{this.tags}}</p>
      </a>
    </div>
    {{/each}}
  </section>
</main>